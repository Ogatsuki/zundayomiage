# 設計ドキュメント (FCIS+SMAC対応)

## 📖 このドキュメントについて
- アプリの要件・概要を記載する設計ドキュメント
- ボトムアップフィードバックで実装から抽出した**S/A評価の「車輪の発明」**を蓄積する

### ボトムアップフィードバックの原則
1. **スクラップ&ビルド**: shell, coreなど層ごとに実装を何度も作り直す（FCIS+SMACの破壊の安全性）
2. **評価と抽出**: ユーザーが納得し指示を出したら実装からS/A評価の設計判断を抽出
3. **ドキュメント更新**: 抽出した「発明」をこのドキュメントに追記
4. **齟齬修正**: 実装とドキュメントの食い違いは実装側に合わせる（実装が正）

### 評価基準
- **S評価（必須追記）**: 設計判断の分岐点、WHYを含む、普遍的な知見
- **A評価（推奨追記）**: プロジェクト固有だが重要な設計判断
- **B/C評価（追記不要）**: 実装から自明、型定義、UI詳細、一般的な技術選定

### Attention分散防止
各担当AI（Shell/State/Core）は以下のみ読む:
- **全体設計（必読）**: アプリの要件・概要。全層が知るべき契約と方針
- **自分の層のセクション**: その層のS/A評価発明
- 多層のセクションの存在は忘れて構わない

---

## **1. 全体設計（全担当者共通）**

**【全担当AI必読】このセクションは全ての層の実装者が参照します。**

### 1.1 プロジェクト概要
**[アプリ名とコンセプトを簡潔に記述]**

### 1.2 技術スタック
- **Framework**: Next.js (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Icons**: Lucide React
- **State Management**: Zustand (In-memory)
- **AI Backend**: OpenAI `gpt-4o-mini`

### 1.2 アーキテクチャ原則
- **FCIS+SMAC**: Pure Functions (Core) + State Machines (State) + Thin Shell
- **層分離**: Core/State/Shell の責務を厳密に分離
- **契約駆動**: 層間は types.ts の契約のみで連携
- **スクラップ&ビルド**: 実装は何度も作り直す。知見のみドキュメントに蓄積

---

**以下は層別実装セクションです。担当する層のセクションのみを読んでください。**

---

## **2. Core層のS/A評価の発明（ボトムアップフィードバック保管庫）**

**[実装から抽出した重要な発明をここに追記]**

**✅ 記載例:**
- **トリガー検知の正確性（S評価）**: 末尾キーワード方式により誤検知ゼロ
  - WHY: 文中に「送信」という単語があっても誤検知しない
  - 実装: `String.endsWith()` を使用

---

## **3. State層のS/A評価の発明（ボトムアップフィードバック保管庫）

**[実装から抽出した重要な発明をここに追記]**

**✅ 記載例:**
- **エラーstate統一管理（A評価）**: `error: string | null` で全エラーを一元管理
  - WHY: 複数のエラー状態を個別管理すると複雑化
  - 実装: Zustandストアに一つのerrorフィールド

- **復元機能（A評価）**: 削除・保存したアイテムを元に戻せる
  - WHY: ユーザーの誤操作救済
  - 実装: `deletedItems`, `savedArticles` 配列で履歴保持

---

## **4. Shell層のS/A評価の発明（ボトムアップフィードバック保管庫）

**[実装から抽出した重要な発明をここに追記]**

**✅ 記載例:**
- **自動高さ調整（A評価）**: テキストエリアが入力量に応じて動的に高さ調整
  - WHY: UXの大幅向上。スクロール不要
  - 実装: useEffectで`scrollHeight`を監視

- **コピー成功フィードバック（A評価）**: アイコン変化で成功通知
  - WHY: ユーザーに明確な成功通知
  - 実装: `<Copy />` → `<Check />` に2秒間変化

- **ホバーでボタン表示（A評価）**: 通常時は非表示、ホバー時のみ表示
  - WHY: UIがスッキリ。必要なときだけ表示
  - 実装: `group` + `group-hover:opacity-100`

---

*End of Template*