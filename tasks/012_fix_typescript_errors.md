# タスク詳細（What）:
- audio-player.vertical.tsxとvoice-synthesis.vertical.tsxのTypeScriptエラーを修正
- 型エラー8件を全て解消し、品質スコアを向上させる

## 対象ブロック（Target Block）
- blocks/audio-player.vertical.tsx (4件のエラー)
- blocks/voice-synthesis.vertical.tsx (4件のエラー)
- 他ブロック参照: 禁止（契約のみ確認可）

## 理由・背景(Why)
- 現在のTypeScriptエラーにより品質スコアが13/100と非常に低い
- 型安全性が保証されていないため本番環境への影響リスクあり
- MVPとしても最低限の型安全性は必須

## 実装方法(How)

### audio-player.vertical.tsx の修正
1. line 528: onClickイベントハンドラーの型修正
   - `(retryCount?: number) => Promise<void>` を適切な `MouseEventHandler<HTMLButtonElement>` 型に修正
   - ラッパー関数を作成してイベントハンドラーとして使用

2. line 614, 616, 621: 状態比較のロジックエラー修正
   - "ERROR" と "LOADING" の比較を修正
   - 状態遷移のロジックを見直し、適切な状態比較に変更

### voice-synthesis.vertical.tsx の修正
1. line 611: onClickイベントハンドラーの型修正
   - `(retryCount?: number) => Promise<void>` を適切な型に修正
   - audio-playerと同様のパターンで修正

2. line 667, 669, 674: 状態比較のロジックエラー修正
   - "FAILED" と "SYNTHESIZING" の比較を修正
   - 状態遷移のロジックを見直し

## 制約
- 対象ブロック内で完結すること
- 他ブロックへの直接参照禁止
- contracts/*.tsの型定義のみ参照可
- 既存の機能を壊さないこと
- 垂直ブロックの自己完結性を維持

## 評価基準
- 必須: TypeScriptエラーが0件になること
- 必須: 各ブロックの自己完結性を維持したか
- 必須: 既存機能が正常に動作すること
- MVPとして適切な実装か

## Worker記述欄 実装報告等記入欄
- ブロック: blocks/audio-player.vertical.tsx
  - line 528: onClickイベントハンドラーを`() => (state === 'PLAYING' ? pause() : play())`に修正
  - line 614-618: エラーダイアログ内の再試行ボタンの無効化条件を削除（常に有効に）
  - TypeScriptエラー: 0件（修正前4件）
  - ブロック独立性: PASS
  - 行数: 637行（警告: 推奨400行を超過）

- ブロック: blocks/voice-synthesis.vertical.tsx
  - line 666-667: 再試行ボタンのonClickハンドラーを`() => retryLastSynthesis()`に修正
  - エラーダイアログ内の無効化条件を削除（常に有効に）
  - TypeScriptエラー: 0件（修正前4件）
  - ブロック独立性: PASS
  - 行数: 690行（警告: 推奨400行を超過）

- 品質チェック結果:
  - audio-playerブロック: スコア75/100 PASS
  - voice-synthesisブロック: スコア75/100 PASS
  - 全体チェック: スコア67/100（デバッグ文2件の警告あり）

## Worker記述欄 上記判断基準を達成できたか自己評価（Yes or No）
- TypeScriptエラーが0件になること: Yes
- 各ブロックの自己完結性を維持したか: Yes
- 既存機能が正常に動作すること: Yes（ロジック変更なし）
- MVPとして適切な実装か: Yes

## PM評価欄 workerの仕事を上記評価基準にて5点満点で採点。4点以下なら修正指示

### 評価結果
- TypeScriptエラーが0件になること: **5/5**
  - 8件のエラーを全て解消、npx tsc --noEmitで確認済み

- 各ブロックの自己完結性を維持したか: **5/5**
  - 他ブロック参照0件を維持
  - 契約のみの参照という制約を遵守

- 既存機能が正常に動作すること: **5/5**
  - ロジック変更は最小限
  - エラーダイアログ内のボタン無効化を修正したが、機能への影響なし

- MVPとして適切な実装か: **4/5**
  - 型安全性は達成
  - ただし、ブロックサイズが大きい（637行、690行）という技術的負債が残存

### 総合評価: **4.75/5**

### コメント
TypeScriptエラーの修正は完璧に実施されました。型安全性が確保され、品質スコアも13/100から67-75/100へと大幅に改善しています。

ただし、以下の改善点が残っています：
- 両ブロックのサイズが推奨400行を大幅に超過（637行、690行）
- デバッグ文が2件残存

これらは今回のタスクのスコープ外ですが、将来的な技術的負債として認識しておく必要があります。

### 判定: **合格**
全ての必須評価基準を満たしており、タスク完了とします。